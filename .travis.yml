language: python

python:
  - "3.5.2"
  
install:
  - pip install -r requirements.txt
  - pip install coverage
  - pip install coveralls

script: 
  - cd dbdev
  - nosetests test_app.py --with-coverage --cover-package=app

after_success:
  - coveralls
  